# OS
.DS_Store
# build artifacts
/dist/
# test
coverage.out
# phonewave runtime state on target repository
.phonewave/
# environment-specific local files (must not contain full paths)
*.local.*
# claude local settings
.claude/*.local.*
# git worktrees
.worktrees/
# plan drafts (ephemeral, approved plans are tracked)
docs/plans/*.draft.md
# golang
.gocache/
# firebase local debug log
firebase-debug.log
